import { AboutContent } from '@/components/AboutContent';

export const metadata = {
  title: 'About — KshitijGotiwale',
  description: 'Kshitij Gotiwale — independent art director, brand designer and illustrator.',
};

export default function AboutPage() {
  return <AboutContent />;
}
